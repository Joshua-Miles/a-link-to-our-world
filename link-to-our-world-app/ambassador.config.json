{
    "extends": "@triframe/ambassador/expo-app.config.json",
    "apis": [
        {
            "remoteUrl": "{{process.env.EXPO_PUBLIC_API_URL}}",
            "destination": "./api"
        }
    ]
}